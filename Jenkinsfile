pipeline {
  agent any
  stages {
    stage('checkout') {
      steps {
        echo 'complete the checkout hare krishna'
        build(job: 'krishna786', quietPeriod: 1, wait: true, propagate: true)
      }
    }
  }
  environment {
    jenkinsfile2 = '146'
  }
}